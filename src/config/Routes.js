import Login from "../containers/Login";

const Login_Route = {
  component: Login,
  path: "/login",
};

const Default_Route = {
  component: Login,
  path: "/login",
};

export default [Login_Route, Default_Route];
